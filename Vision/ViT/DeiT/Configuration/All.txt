# Preprocessing Downloads
dataset = load_dataset('tanganke/dtd', cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/dtd
dataset = load_dataset('tanganke/eurosat', cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/eurosat
dataset = load_dataset("tanganke/gtsrb", cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/gtsrb
dataset = load_dataset("ylecun/mnist", cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/ylecun/mnist
dataset = load_dataset('tanganke/resisc45', cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/resisc45
dataset = load_dataset("tanganke/stanford_cars", cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/stanford_cars
dataset = load_dataset('tanganke/sun397', cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/sun397
dataset = load_dataset("ufldl-stanford/svhn", "cropped_digits", cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/ufldl-stanford/svhn

# Fine Tuned, Linear Probe, and Reverse Probe Setups:
1. Trained 5 seperate times within 500 epochs, stopped training if there was no improvement after 25 epochs.
2. AdamW optimizer, encoder learning rate = 1e-5, classifier head learning rate = 1e-3, weight decay = 0.01, and batch size = 8.
3. For Increment Training, datasets were split into 5 equal subsets with the same number of images per label.