dataset = load_dataset('tanganke/dtd', cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/dtd
num_classes = 47
dataset_name = "DTD"
size_nums = [i for i in range(1, 40)] #10
size_nums.append(float('inf'))
fine_tuned_model_name = "tanganke/clip-vit-base-patch32_dtd"

# Best Augmented Accuracy
