dataset = load_dataset('tanganke/eurosat', cache_dir="/workspace/.hf_cache") # https://huggingface.co/datasets/tanganke/eurosat
num_classes = 10
dataset_name = "EuroSAT"
size_nums = [i for i in range(1, 28)] # Start from 1,2,3,4,5,...->27. 27 total. 
for i in range(40, 150, 10): # Adds {40, 50, 60, ...140}. 11 + 27 -> 38.
    size_nums.append(i)
size_nums.append(300) # 39. 
size_nums.append(600) # 40
size_nums.append(1200) # 41

fine_tuned_model_name = "tanganke/clip-vit-base-patch32_eurosat"

# Best Augmented Accuracy:
